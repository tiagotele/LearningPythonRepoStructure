# LearningPythonRepoStructure

This is a repo for keep as reference on how to strcure a Python codebase. Here is possible find use for unit testing, Makefile. 

For run unittests use
```
python -m unittest
```

It's under progress.
